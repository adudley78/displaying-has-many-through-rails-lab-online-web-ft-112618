<h1><%= @doctor.name %></h1>
<h2><%= @doctor.department%></h2>
<p>
  <% @doctor.appointments.each do |appointment| %>
    <%= link_to appointment.patient.name, patient_path(appointment.patient) %> | <%= appointment.appointment_datetime.strftime("%B %e, %Y at %H:%M") %>
  <% end %>
</p>
