<h1><%= @patient.name %></h1>
<p>
  <% @patient.appointments.each do |appointment| %>
  <%= link_to appointment.doctor.name, doctor_path(appointment.doctor) %> | <%= appointment.appointment_datetime.strftime("%B %e, %Y at %H:%M") %>
  <% end %>
</p>
